/*Header Guard, so that no functions are defined 
 *twice if included multiple times*/
#ifndef DIRECTIVES_H
#define DIRECTIVES_H

/*Good practice is not to define functions in headers, only prototypes*/
int add(int one, int two);

#endif

//#pragma once   also works, but not on all compilers